class Section {
  constructor({ items, renderer }, container) {}

  renderItems() {
    // renders all elements on page
  }

  additem() {
    // takes a DOM element
    // adds to container
  }
}

export default Section;
