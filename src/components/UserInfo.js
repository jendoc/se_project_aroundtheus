class UserInfo {
  constructor({ name, job }) {}

  getUserInfo() {
    // returns object with info about user
  }

  setUserInfo() {
    // takes new user data
    // adds to page
  }
}

export default UserInfo;
